import SwiftUI
import SwiftData

struct SettingsView: View {
    
    var body: some View {
        VStack(spacing: 20) {
            Button("Sort Channels") {
                
            }
            Button(".m3u Links") {
                
            }
        }
        .padding()
        .frame(maxWidth: .infinity)
    }
}

#Preview {
    Root()
}
